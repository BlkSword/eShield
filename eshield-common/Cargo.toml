[package]
name = "eshield-common"
version = "0.1.0"
edition = "2021"
license = "Apache-2.0"
description = "Shared types and constants between eBPF and userspace"

[features]
default = []
userspace = ["aya"]

[dependencies]
aya = { workspace = true, optional = true }
